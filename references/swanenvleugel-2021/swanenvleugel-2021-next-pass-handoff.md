# Next-pass handoff: Swanenvleugel clean extraction upgrade

Use this if someday upgrading the extraction beyond the current `complete-working` / `appendices-cleanup-complete` state.

## Goal

Improve the existing standalone Swanenvleugel package while keeping it a clean, readable Markdown extraction. Do not convert it into a diplomatic transcription.

## Priority checks

1. Form-level notation audit: check starred reconstructions with laryngeals, syllabic resonants, glides, long vowels, fricatives, and glottalic marks.
2. Full main-text form-level audit: the appendices have already been checked, but the body examples still need a pass if canonical form-level accuracy is desired.
3. TAM table enrichment: optionally add examples, branch-specific reflexes, and certainty notes from sections 7–9.
4. Footnote cleanup: convert to Markdown footnotes only if useful.
5. Heading/navigation polish: confirm all numeric sections are headings.

## Done criteria

Mark as `complete-clean` only when the main text and TAM material have been checked at form level, remaining notation artifacts are resolved or documented, and `swanenvleugel-2021-notes.md` records remaining limitations.
