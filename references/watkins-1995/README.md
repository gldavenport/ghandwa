# Watkins 1995 practical pass package

This package contains the practical follow-up pass requested after the item 1/item 2 pass.

Core deliverables are in `practical-pass/`:

- improved normalized occurrence and unique-form datasets
- a log resolving all 191 previously flagged structured-form suspect rows
- a remaining-suspects audit
- a short Greek/index human-collation shortlist
- a practical-pass report and JSON summary

The latest main Markdown from the prior package is included at the top level as `watkins-1995-how-to-kill-a-dragon.md`. This pass primarily improves companion datasets, not the main prose extraction.
